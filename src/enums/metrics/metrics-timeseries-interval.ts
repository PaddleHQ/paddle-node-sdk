/**
 * Granularity of metrics timeseries returned by the Paddle Billing metrics API.
 * Add new members here when the API supports additional intervals.
 */
export enum MetricsTimeseriesInterval {
  Day = 'day',
}
