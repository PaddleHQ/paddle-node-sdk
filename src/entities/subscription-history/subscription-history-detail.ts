/**
 *  ! Autogenerated code !
 *  Do not make changes to this file.
 *  Changes may be overwritten as part of auto-generation.
 */

import { convertKeysToCamelCase } from '../../internal/base/index.js';
import { type ISubscriptionHistoryDetailResponse } from '../../types/index.js';

type SnakeToCamelCase<Value extends string> = Value extends `${infer First}_${infer Rest}`
  ? `${First}${Capitalize<SnakeToCamelCase<Rest>>}`
  : Value;

export type DeepCamelCase<Value> =
  Value extends Array<infer Item>
    ? Array<DeepCamelCase<Item>>
    : Value extends object
      ? {
          [Key in keyof Value as Key extends string ? SnakeToCamelCase<Key> : Key]: DeepCamelCase<Value[Key]>;
        }
      : Value;

export type SubscriptionHistoryDetail = DeepCamelCase<ISubscriptionHistoryDetailResponse>;

export function hydrateSubscriptionHistoryDetail(
  detail: ISubscriptionHistoryDetailResponse,
): SubscriptionHistoryDetail {
  return convertKeysToCamelCase(detail) as SubscriptionHistoryDetail;
}
