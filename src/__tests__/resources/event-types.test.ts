/**
 *  ! Autogenerated code !
 *  Do not make changes to this file.
 *  Changes may be overwritten as part of auto-generation.
 */

import { getPaddleTestClient } from '../helpers/test-client.js';
import { ListEventTypeMockResponse } from '../mocks/resources/event-types.mock.js';
import { EventTypesResource } from '../../resources/index.js';

describe('EventTypesResource', () => {
  test('should return a list of events', async () => {
    const paddleInstance = getPaddleTestClient();
    paddleInstance.get = jest.fn().mockResolvedValue(ListEventTypeMockResponse);

    const eventsResource = new EventTypesResource(paddleInstance);
    const eventTypes = await eventsResource.list();

    expect(paddleInstance.get).toBeCalledWith('/event-types');
    expect(eventTypes.length).toBe(31);
  });
});
